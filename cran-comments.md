## R CMD check results

0 ERRORs | 0 WARNINGs | 2 NOTEs

* NOTE: New submission.
* NOTE: Skipping checking HTML validation (HTML Tidy version issue on
  the local machine; not a package issue).

## Test environments

* local macOS (aarch64-apple-darwin20), R 4.5.1

## Downstream dependencies

This is a new package with no downstream dependencies.
